// Entry point removed — this project has been migrated away from Remotion.
// The Remotion `Composition`/`registerRoot` entry has been replaced by
// a headless ffmpeg + canvas pipeline in `src/processBatch.js`.

module.exports = {};
