// generate-inputprops.js: REMOVED — this helper previously created Remotion
// `inputProps` JSON files for Remotion Cloud. The project has been migrated
// away from Remotion so this script is intentionally disabled.
console.log('generate-inputprops.js: REMOVED — project is now Remotion-free.');
